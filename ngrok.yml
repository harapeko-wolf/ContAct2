version: "2"
# authtoken is set via environment variable or docker-compose
tunnels:
  backend:
    addr: nginx:80
    proto: http
    inspect: false
  frontend:
    addr: frontend:3000
    proto: http
    inspect: false
web_addr: 0.0.0.0:4040 